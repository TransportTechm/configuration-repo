message: testing zull on test instance
info:
  component: Edge Server
  
endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: false

zuul:
  prefix: /api
  routes:
    customer: 
      path: /customer/**
      #serviceId: customerservice
      url: http://INBASDLP00683.TechMahindra.com:8080/          
    transport:
      path: /transport/**
      serviceId: TransportServiceT
    vendor:
      path: /vendor/**
      serviceId: VendorServiceT
    employee:
      path: /employee/**
      url: https://employeeservicetechm.cfapps.io/
  ratelimit:
    enabled: true
    policies:
      customer:
        limit: 3
        refresh-interval: 10 
      vendor:
        limit: 3
        refresh-interval: 10     
      transport:
        limit: 3
        refresh-interval: 10 
ribbon:
  eureka:
    enabled: true

eureka:
  client:
    registerWithEureka: false
      
server:
  port: 8765

logging:
  level:
    ROOT: INFO
    org.springframework.web: DEBUG
    
security:
  basic:
    enabled: false
  ignored: /**
  
management:
  security:
    enabled: false
  